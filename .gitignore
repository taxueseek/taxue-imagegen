# Local personalization — never commit
memory/
user-preferences.md

# Local-only archive — never published. Holds material kept off the public
# repo; scripts/run_tests.sh verifies this directory is never tracked.
_research/

# Local test artifacts
_backup_*
test-prompts.md
*.png.tmp

# Generated prompt batches (build_storyboard output — regenerate on demand)
scripts/_prompts/
scripts/_prompts_ink/
# 已验证风格的提示词原文归档——与真源侧 .gitignore 对齐
scripts/_prompts_archive/

# Local run log (postcheck writes here; not a source artifact)
scripts/logs/
scripts/_calib_cache/
scripts/wm_alpha_1024.npz.bak-*

# OS
.DS_Store

# Local archive
.trash/
__pycache__/

# Python virtualenv / build
.venv/
*.egg-info/
build/
dist/

# Release build artifacts — produced by .github/workflows/release.yml (in CI) and
# uploaded as release assets. Never commit them from a local dry-run.
/taxue-imagegen-v*.zip
/SHA256SUMS
/RELEASE_NOTES.md

# Editor / IDE
.vscode/
.idea/
*.swp